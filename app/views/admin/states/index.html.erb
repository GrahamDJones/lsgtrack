<%= link_to "New State", new_admin_state_path %>

<ul>
  <% for state in @states %>
      <li id="state_<%= state.id %>">
        <%= state.name %><%= " #{state.default ? '(default)' : ''}" %>
        <% if !state.default %>
            <%= link_to "Make default", make_default_admin_state_path(state) %>
        <% end %>
            </li>
        <% end %>
        </ul>