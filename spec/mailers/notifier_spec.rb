require "spec_helper"

describe Notifier do
  # combined test into comment_spec.rb
  #it "correctly sets the reply-to" do
  #  comment = FactoryGirl.create(:comment)
  #  mail = ActionMailer::Base.deliveries.last
  #  mail.from.should eql(["lsg.tracker+#{comment.project.id}+#{comment.ticket.id}@gmail.com"])
  #end
end
